import type { Config } from 'next'

const config: Config = {
  metadata: {
    title: 'VORA - عطور فاخرة',
    description: 'منصة متخصصة في بيع العطور الفاخرة والأصلية',
    viewport: 'width=device-width, initial-scale=1',
    themeColor: '#d97706',
  },
}

export default config
